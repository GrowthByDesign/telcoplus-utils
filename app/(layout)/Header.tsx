import Container from "../(components)/Container";
import TopBar from "./TopBar";

const Header = () => {
  return (
    <Container>
      <div>Header</div>
      <TopBar />
    </Container>
  );
};

export default Header;
